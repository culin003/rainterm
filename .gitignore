# Gradle
.gradle/
build/
!gradle-wrapper.jar
.gradleBuild/

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
nbproject/

# OS
.DS_Store
Thumbs.db

# Logs / runtime
*.log
*.tmp

# Raindrop runtime data (contains credentials)
.raindrop/
*.db
*-shm
*-wal
*-journal

# Encryption keys
*.key

# Local secrets — never commit. Holds the GitHub PAT used for push/release.
.env
.env.*
!.env.example
.secrets

# Machine-local Gradle JDK override — holds an absolute java.home path that
# would break CI runners. The repository's JDK is provided by setup-java there.
gradle.properties

# AI tooling scratch dir (Serena). Plans under .mimocode/ are committed; this is not.
.serena/
