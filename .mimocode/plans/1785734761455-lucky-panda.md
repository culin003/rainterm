# 发布 Raindrop v1.2.0

## 前置检查

- `.env` 已正确 gitignored，不会泄露 GitHub PAT
- `.mimocode/plans/` 目录**不应提交**到 git
- `origin/main` 在 `a7fde2d`（v1.0.1），本地领先 8 个提交

## 执行步骤

### Step 1: 审查未提交改动
查看 18 个文件的改动内容，确认没有意外改动，并总结 release notes。

### Step 2: 提交未提交的改动
- **不提交** `.mimocode/plans/1785734761455-lucky-panda.md`
- Stage 17 个已修改文件 + 1 个新文件 `TerminalPanelInternals.java`
- Commit message: 描述终端字体渲染、设置面板增强等改动

### Step 3: 更新 RELEASE_NOTES.md
在文件顶部追加 v1.2.0 更新内容，基于 Step 1 审阅的改动摘要。

### Step 4: 升级版本号
`build.gradle.kts` 第 13 行: `version = "1.1.0"` → `version = "1.2.0"`

### Step 5: 提交版本变更
Commit message: `Bump version to 1.2.0`

### Step 6: 推送代码和标签到 origin
```bash
git push origin main        # 推送所有 10 个提交
git push origin v1.0.2      # 推送已有 tag
git push origin v1.0.3
git push origin v1.1.0
```

### Step 7: 创建并推送 v1.2.0 tag
```bash
git tag -a v1.2.0 -m "Raindrop v1.2.0"
git push origin v1.2.0
```

推送后 `.github/workflows/release.yml` 自动触发 4 平台构建（amd64/arm64/x64/aarch64）并创建 GitHub Release。

### Step 8: 验证
- 检查 `git log --oneline --decorate` 确认 master 在 v1.2.0
- 检查 GitHub Actions 确认 release workflow 已触发

## 关键文件
| 文件 | 操作 |
|------|------|
| `build.gradle.kts` | 改版本号 |
| `RELEASE_NOTES.md` | 追加 v1.2.0 更新内容 |
| 18 个源文件 | 提交未提交的改动 |
| `.mimocode/plans/*.md` | **排除**（不提交） |
